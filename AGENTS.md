# AGENTS.md

## 项目背景

这是一个本地优先的桌面内容生产工具，最初用于按句录制视频配音，后续逐步扩展为一个围绕“分片内容整理、录音、素材打包、导出”的桌面应用。

这个项目不是一次性的脚本工具，而是会持续迭代的正式项目。

当前可以把它理解为：

- 面向个人内容生产流程的桌面工具
- 强调本地存储、本地处理、本地导出
- 服务于后续音频整理、素材整理、剪辑协作等工作流

## 总体约束

- 默认坚持本地优先，不依赖远程数据库或远程后端
- 优先使用本地文件系统进行数据存储和导出
- 所有实现都应以“可维护、可扩展、可调试”为前提
- 不要把它当成临时 demo 来写

## 开发规范

### 1. 文件必须控制体积

这是最重要的约束。

- 不要把过多逻辑堆进单个文件
- 新功能默认先考虑模块拆分
- 如果一个文件已经明显偏大，就不要继续往里塞新功能

建议标准：

- 理想长度：`300` 行以内
- 软上限：`400` 行
- 硬上限：`500` 行

如果超过软上限，默认应该拆分。

### 2. 单文件职责单一

- 一个文件只负责一类事情
- 不要把 UI、状态、存储、导出、业务逻辑混在一起
- 重复逻辑要尽早抽离
- 公共逻辑要集中，不要四处分散复制

### 3. 优先模块化，而不是补丁式堆叠

- 新增功能时，优先建立清晰边界
- 少做超长的条件分支和临时兼容层
- 少做“先拼上去再说”的实现
- 临时方案如果出现，后续要尽快回收

### 4. 显式优先

- 优先简单直接的数据结构和函数
- 避免隐式副作用
- 避免难以理解的魔法逻辑
- 命名、排序、导出规则要稳定且可预测

### 5. 保持兼容和稳定

- 修改本地数据结构时，尽量兼容旧数据
- 不要轻易做破坏性修改
- 不要误覆盖用户原始文件
- 导出逻辑必须稳定、可重复

### 6. UI 以效率为先

- 界面首先服务于操作效率
- 不要为了“好看”牺牲可用性
- 长耗时操作必须给出明确状态反馈
- 模式切换时，只展示当前模式需要的控件

### 7. 每次改动都要做基础验证

至少要做：

- 对改动过的 JS 文件执行 `node --check`
- 执行一次 `npm start` 启动烟测

如果改动涉及导出、文件处理、排序、命名，还要额外确认实际输出是否符合预期。

## 决策原则

如果有两种做法：

- 为了快，继续往大文件里堆代码
- 多花一点时间，先拆清边界再实现

默认选择后者。

除非改动真的非常小，否则不要继续恶化项目结构。
